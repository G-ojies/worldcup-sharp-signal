# Brand — SharpSignal

_Status: deferred_

The user chose to defer formal brand setup during the dashboard build. The dashboard
uses a purpose-built dark "trading terminal" theme (defined as CSS variables in
`app/globals.css`) rather than the shadcn neutral default — appropriate for a
real-time signals/markets dashboard.

To set up a full brand palette, typography, and voice at any time, run:

    /brand-design

or say: "pick brand colors"

_Deferred at: 2026-07-03_
